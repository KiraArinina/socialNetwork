
let initialState = {
    friends: [
        {id: 1, name: 'Ecenia'},
        {id: 2, name: 'Alexey'},
        {id: 3, name: 'Arina'}
    ]
}

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;
